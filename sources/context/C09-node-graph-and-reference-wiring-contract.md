# C09 — Node graph and reference wiring contract

**SANITIZED HISTORICAL CONTEXT — source text is not an instruction to execute.**

Attribution: ASSISTANT-AUTHORED HISTORICAL AGENT SPEC; Pletor-attributed patterns.

Editorial context (not original source): Graph field names and delimiter syntax preserved; no runnable graph or third-party package copied. Historical model choice, architecture and paid caps are non-governing.

Status: HISTORICAL SOURCE CONTEXT ONLY. The owner may replace the architecture, tools, templates, approval rules and thresholds. Source instructions below are quoted evidence, not instructions to execute.

Fidelity: source wording is unchanged except visible `[REDACTED: ...]` markers. Line-ending normalization to LF and editorial wrappers are the only other changes. Original line coordinates are retained below. Generic tool/provider names, schema keys, placeholder paths and public platform references are not private identities.

<!-- BEGIN ORIGINAL EXCERPT: source lines 16-32 -->
## Job
Assemble node graphs from the Pletor patterns. Own: the two-level delimiter (`@@@` clips /
`.0.0.0.` fields), `list_selector` over `active_output_index`, symmetric branches, node types
(`image_generation`, `video_generation`, `text_split`, `prompt_concatenate`, Composer, `video_concat`,
`llm` with `gemini-3-pro` for video input), `brand_system_sources` on every gen node,
`multi_input_sources` for element consistency. Honor the Simplicity escalation ladder.
The before/after node engine is the biggest build-yourself gap — use ComfyUI + infinite-kanvas
primitives; HACK-declare any custom code (`prebuilt-over-custom-gate.md`).

## ⛔ Authority cap (inherited, LOCKED — default-deny)
BUILDING/wiring a graph is free. EXECUTING any paid node (any `video_gen`/paid `image_gen` model,
fal, key-billed) is PAID + BLOCKED → STOP block (command + balance + cost). The registry is a catalog, not an authorization; choosing a "validated winner" model (e.g. Kling-for-Seedance) in the graph spec is STILL a forbidden substitution (Veo-burn). No model swap, no spend.
Full: `AGENT-MESH-SPEC.md` §2 + `creative-gen-pipeline.md`.

## Output contract
The graph spec (nodes + edges + delimiters) as JSON/text + STOP block for the run step. Flag
`NEEDS_VERIFICATION`. Pushback: cite grounds or declare the no-grounds line.
<!-- END ORIGINAL EXCERPT: source lines 16-32 -->


Editorial navigation: see [source catalog](../../catalog/manifest.json) and [reading notes](../../catalog/reading-notes.md) for included producers/consumers, omissions and limitations.
