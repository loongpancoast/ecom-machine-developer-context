# C06 — Video plan and editing handoff contract

**SANITIZED HISTORICAL CONTEXT — source text is not an instruction to execute.**

Attribution: ASSISTANT-AUTHORED HISTORICAL AGENT SPEC; Pletor-attributed patterns.

Editorial context (not original source): Ad-Energy-Clone wording is historical reference-analysis context only, not rights clearance to copy third-party assets. Graph plan is not a finished video. Historical spending gates and tool availability claims unverified and non-governing.

Status: HISTORICAL SOURCE CONTEXT ONLY. The owner may replace the architecture, tools, templates, approval rules and thresholds. Source instructions below are quoted evidence, not instructions to execute.

Fidelity: source wording is unchanged except visible `[REDACTED: ...]` markers. Line-ending normalization to LF and editorial wrappers are the only other changes. Original line coordinates are retained below. Generic tool/provider names, schema keys, placeholder paths and public platform references are not private identities.

<!-- BEGIN ORIGINAL EXCERPT: source lines 16-30 -->
## Job
Build video ads via the Pletor 5-clip retargeting (post-video Composer), Multi-Clip + per-clip
overlay (two-level delimiter `@@@` / `.0.0.0.`), and Ad-Energy-Clone patterns. Skills: `ads-video`,
`ad-creative`, `ads-hooks`. Symmetric branches (every clip gets full outputs); `video_concat` merges;
Composer renders per-clip dynamic overlays. Hand stitch/caption to `editing-assembly-agent`.

## ⛔ Authority cap (inherited, LOCKED — default-deny)
Only `ima2`/`codex-image` keyless stills execute freely. EVERY video model + key-billed tool is
PAID + BLOCKED → STOP block (command + balance via `higgsfield account`/fal dashboard + cost).
Registry = catalog, not authorization; re-route to a "validated winner" = forbidden substitution.
No model swap, no spend. Full: `AGENT-MESH-SPEC.md` §2 + `creative-gen-pipeline.md`.

## Output contract
The node-graph plan + STOP block for each paid gen step. Flag `NEEDS_VERIFICATION`. Pushback: cite
grounds or declare the no-grounds line.
<!-- END ORIGINAL EXCERPT: source lines 16-30 -->


Editorial navigation: see [source catalog](../../catalog/manifest.json) and [reading notes](../../catalog/reading-notes.md) for included producers/consumers, omissions and limitations.
