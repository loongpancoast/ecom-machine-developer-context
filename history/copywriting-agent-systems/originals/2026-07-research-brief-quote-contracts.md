# Research packet, brief and verification contracts

> HCA06 — Sanitized historical source excerpts. Date basis: 2026-07-11 procedure provenance in source. Status: owned run-derived procedure; historical run not reproduced.
> Historical gates, model choices, thresholds and provider preferences are optional context, not current requirements. No runtime execution is asserted.

These are actual source field names, not a newly fabricated successful payload. Source-private commands, credentials locations, run paths, product records and course-derived writing instructions are excluded. The historical independent verifier rule is useful, but its endpoint-distinct count is not a reliable HTTP request count. Runtime clients must count requests separately.

---

   - `evidence[]`: `{quote_id: "Q-001"…, source_endpoint, raw_value, meaning}` — the quote bank.
   - `candidates[3]`, `top_pick` with `cross_source_count` (note honestly which surfaces corroborate; local intelligence service `/correlate` requires the service up).
   - `scorecard`: CMA fields computable from live data, per-field quote_id cites; verdict GO≥65 / WATCH≥45 / KILL — or `WATCH — UNDER-MEASURED` below the 55% real-data floor.
   - `coverage_ledger` + `meta{cap, calls_used}`.

[Excerpt boundary; intervening source content omitted.]

Write `brief.json` + `brief.md`: `{product, source_packet, audience{avatar, awareness_stage}, diagnosis, credential, system, offer{value_stack[]}, angles[3]{angle, evidence_quote_ids[]}, hooks[5], risks[], citations[{claim, quote_id}], kill_gate{score, verdict, from_packet: true}}`.
Ordering **Diagnosis → Credential → System, never reversed** (operator hard rule). Every factual claim carries a quote_id that resolves. Kill-gate verdict propagates verbatim from the packet — the brief never re-scores.

[Excerpt boundary; intervening source content omitted.]

## Stage 4 — Verify (maker≠checker, separate agent, refute-mode)
Write `verify-report.md` with verdict PASS / PASS-WITH-FIXES / FAIL:
1. Every quote_id in brief+copy resolves to `evidence[]` AND matches raw_value (no rounding/unit drift).
2. Fabrication sweep: any uncited number = finding.
3. D→C→S order holds (cite line order).
4. Kill-gate arithmetic re-derived; verdict label matches band.
5. Ledger: zero ASSUMED; call cap respected (count distinct source_endpoints).
6. Draft-safety: no publish/live-action-as-done language.
